# Project-scoped Codex defaults. Loaded only when this repository is trusted.
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
personality = "pragmatic"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
# Framework, provider, API, and deployment behavior remain time-sensitive while
# productizing the API-first web MVP.
web_search = "live"

[features]
apps = true
goals = true
hooks = true
multi_agent = true
shell_snapshot = true
unified_exec = true

# Project-local replacement for the Build iOS Apps plugin's simulator MCP.
# Its nine MIT-licensed workflow skills are vendored under .agents/skills so
# this repository does not depend on a globally enabled plugin.
[mcp_servers.xcodebuildmcp]
command = "npx"
args = ["-y", "xcodebuildmcp@2.6.2", "mcp"]
enabled = true
startup_timeout_sec = 30
tool_timeout_sec = 120

[mcp_servers.xcodebuildmcp.env]
XCODEBUILDMCP_ENABLED_WORKFLOWS = "simulator,ui-automation,debugging,logging"

[agents]
# GPT-5.6 Sol root coordinator plus at most three concurrent support agents.
# Bounded web/API implementation, verification, and validation roles pin Terra;
# reserve Sol roles for hard Apple work and material final risk review.
max_threads = 4
# Direct children only; avoid uncontrolled recursive fan-out.
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true
