{
  "name": "honeymoon-period-cloudflare-api-prototype",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local --port 8788",
    "migrate": "wrangler d1 migrations apply hp-prototype --local",
    "smoke": "./smoke.sh"
  },
  "devDependencies": {
    "wrangler": "4.111.0"
  }
}
